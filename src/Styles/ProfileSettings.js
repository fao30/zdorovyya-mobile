import { StyleSheet, Text, View, Button } from "react-native";

export const profileSettings = StyleSheet.create({
  headerFont: {
    color: "#08024B",
    fontWeight: "bold",
    fontSize: 20,
  },
});
