import { StyleSheet, Text, View, Button } from "react-native";

export const profileFamily = StyleSheet.create({
  content: {
    height: "100%",
    justifyContent: "center",
    alignItems: "center",
  },
  contentText: {
    fontWeight: "400",
    fontSize: 16,
    textAlign: "center",
  },
});
